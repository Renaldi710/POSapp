APP_NAME=POSapp
APP_ENV=local
DEBUG=true

# SQLite (local dev) — auto-configured when APP_ENV=local or development
# DATABASE_URL=sqlite+aiosqlite:///./database.sqlite

# Neon/Supabase (production)
# DATABASE_URL=postgresql+asyncpg://postgres:<password>@db.<project-ref>.neon.tech:5432/postgres

CORS_ORIGINS=http://localhost:3000,http://localhost:5173
